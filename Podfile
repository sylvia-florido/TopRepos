platform :ios, '9.0'

target 'TopRepos' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TopRepos
  pod 'lottie-ios'

  target 'TopReposTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
